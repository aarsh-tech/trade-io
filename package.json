{
  "name": "algo-trade",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:generate": "prisma generate --schema=./prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema=./prisma/schema.prisma",
    "db:push": "prisma db push --schema=./prisma/schema.prisma",
    "db:studio": "prisma studio --schema=./prisma/schema.prisma",
    "db:sqlite:generate": "prisma generate --schema=./prisma/schema.sqlite.prisma",
    "db:sqlite:push": "prisma db push --schema=./prisma/schema.sqlite.prisma",
    "desktop:build": "pnpm --filter @algo-trade/desktop build:main",
    "desktop:dist": "pnpm --filter @algo-trade/desktop dist",
    "desktop:dist:portable": "pnpm --filter @algo-trade/desktop dist:portable"
  },
  "devDependencies": {
    "turbo": "2.3.3",
    "typescript": "5.7.3",
    "prettier": "3.4.2",
    "@types/node": "22.10.7"
  },
  "dependencies": {
    "@prisma/client": "6.2.1",
    "prisma": "6.2.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}